services:
  postgres:
    image: postgres:16-alpine
    container_name: meu_postgres

    environment:
      POSTGRES_DB: criptodb
      POSTGRES_USER: admindb
      POSTGRES_PASSWORD: 06041996Vv!

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: